const { Command } = require('discord.js-commando');

module.exports = {
	name: 'addunit',
	description: 'WIP Command - Add a unit to the current game',
	execute(message, args) {
		message.channel.send('Error: While this is a valid command, it has not yet been implemented because GT is lazy AF\n(Also he is still learning how to code, but we will just ignore that in order to bully him)');
	},
};