const { Command } = require('discord.js-commando'); // Only include if you want to check for permissions!

module.exports = {
	name: 'reload',
	description: 'Reloads a command',
	execute(message, args) {
		// ...
	},
};